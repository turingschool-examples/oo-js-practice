var assert = require('chai').assert;
var Employee = require('../lib/employee');

describe('Employee', function() {

  it.skip('should be have a name, age, and salary', function() {
    // NOTE: It should only take one argument
  });

  it.skip('should be able to have a different name, age, and salary', function() {

  });

});
